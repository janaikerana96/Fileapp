.wrapper_with_padding 
  #note_show
    %h1.title= @doc.title
    %p= simple_format(@doc.content)

    .buttons 
      = link_to "Edit Doc", edit_doc_path(@doc), class: "buttons"
      = link_to "Trash it ", doc_path(@doc), method: :delete, data: { confirm: "Are you sure?"}, class: "buttons"